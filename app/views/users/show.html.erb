<% provide(:title, @user.name) %>
<div class="row">
	<aside class="span4">
		<section>
			<h1>
				<%= gravatar_for @user %>
				<%= @user.name %>, <%= mail_to @user.email, @user.email, target: '_blank' %>
			</h1>
		</section>
	</aside>
</div>